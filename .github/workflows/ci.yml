name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - grid-query-hardening
      - owner-lifecycle-hardening

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: grid
          POSTGRES_PASSWORD: grid
          POSTGRES_DB: grid
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U grid"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://grid:grid@localhost:5432/grid
      TEST_DATABASE_URL: postgresql://grid:grid@localhost:5432/grid
    steps:
      - uses: actions/checkout@v4
      - name: Checkout backend schema copy
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/FHH_VR-VISUALISIERUNGSPLATTFORM_BACKEND
          ref: ${{ (github.head_ref || github.ref_name) == 'owner-lifecycle-hardening' && 'owner-lifecycle-hardening' || 'main' }}
          token: ${{ secrets.SCHEMA_PARITY_TOKEN || github.token }}
          path: .schema-peer
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm zenstack-generate
      - run: git diff --exit-code
      - run: pnpm schema:check-parity
        env:
          ZMODEL_PEER_DIR: ${{ github.workspace }}/.schema-peer/zmodel
      - run: pnpm prisma db push --skip-generate
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm build
